version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 24
    commands:
      - echo Installing dependencies...
      - npm ci

  build:
    commands:
      - echo Build started on `date`
      - echo Node.js application build completed

artifacts:
  files:
    - '**/*'
